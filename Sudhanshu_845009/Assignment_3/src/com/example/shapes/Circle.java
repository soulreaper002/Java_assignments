package com.example.shapes;

public class Circle {
	void calculateArea(int sideLength)  {
		float area = (float)(( sideLength * sideLength)*3.14);
 		System.out.println("The Area of the Circle is "+area);
	}
}
